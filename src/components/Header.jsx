import React from 'react';
import HeaderImage from './header/HeaderImage'

export default function Header(pokemon) {
    // const info = pokemon.map(inf => (inf));
    // console.log(info[1]);
    return (
        <div>
            <HeaderImage />
        </div>
    )
}
